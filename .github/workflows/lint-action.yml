name: Eslint

on:
  push:
    branches: [ $dev ]
  pull_request:
    branches: [ $dev ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3
      
      - name: Install dependencies
      - run: npm i

      - name: Run linter
        run: npm run lint
